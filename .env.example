# Postgres
POSTGRES_USER=etl_user
POSTGRES_PASSWORD=etl_password
POSTGRES_DB=etl_db
POSTGRES_PORT=5432

# Pipeline config
CSV_PATH=/data/input.csv
INTERVAL_MINUTES=60

# PII salt (HMAC). Change for production. Keep secret.
PII_SALT=my_super_secret_salt

# Database URL optional - left blank to build from the vars above.
#DATABASE_URL=
